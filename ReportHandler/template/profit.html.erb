<!DOCTYPE html>
<html class="no-js" lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="shortcut icon" href="/favicon.ico">

    <meta name="author" content="">
    <meta name="description" content="">
    <meta name="keywords" content="">
    
    <title></title>

    <link href="./css/libs/bootstrap.min.css" rel="stylesheet">
    <script src="./js/libs/modernizr-2.7.1.min.js"></script>

    <link href="./css/main.css" rel="stylesheet">
</head>
<body>
<div class="container">

<div class="row">
	<div class="col-xs-12">
    <table class="table table-fixed-height">
			<tbody>
				
				<% data[:cb_year].each_with_index do |yr,n| %>
				  <tr>
				    <th>Profit <%= yr %> <%= data[:cb_crop][n] %> 
				      <!--<br> Min: <%= "$ %.2f" % data[:cb_min][n] %>
				      <br> Max: <%= "$ %.2f" % data[:cb_max][n] %>--> 
				    <div class="budget-font">Avg ($/ac): <%= "$ %.2f" % data[:cb_average][n] %></div>
				    </th>
				    <td>
				      <div class="budget-frame">
				      <img src="../report_data/budget_hist_<%=n%>.svg" class="histogram" ></td>
				      </div>
				    <td>
				      <div class="budget-frame">
				      <img src="../report_data/budget_<%=n%>.png" class="budget-map">
				      </div>
				    </td>
  					<td>
  					  <div class="budget-legend-frame">
  					  <img src="../report_data/profit_legend.png" class="budget-legend">
  					  </div>
  					</td>
				  </tr>
				<% end %>
				
				<tr>
				    <th>Multi-Year Average Profit 
				    <div class="budget-font">Avg ($/ac): <%= "$ %.2f" % data[:cb_multi_year_average] %> </div>
				    </th>
				    <td>
				      <div class="budget-frame">
				      <img src="../report_data/budget_hist_cbaverage.svg" class="histogram" ></td>
				      </div>
				    <td>
				      <div class="budget-frame">
				      <img src="../report_data/budget_average.png" class="budget-map">
				      </div>
				    </td>
  					<td>
  					  <div class="budget-legend-frame">
  					  <img src="../report_data/profit_legend.png" class="budget-legend">
  					  </div>
  					</td>
				  </tr>
        
			</tbody>
		</table>
	</div>
</div>

<div class="row dont-break">
	<div class="col-xs-12">
    <table class="table table-small-text table-striped table-bordered">
      <thead>		
		    <tr>
		      <th>Map Unit Symbol</th>
		      <% data[:yield_mapunit][0]['yield'].each do |yit| %>
		        <th><%= yit['year'] %> <%= yit['crop'] %> yield (bu/ac)</th>
		      <% end %>
		    </tr>
		  </thead>
		  
		  <tbody>
			  <% data[:yield_mapunit].each do |it| %>
			  <tr>
		      <td><%= it['musym'] %></td>
		      <% it['yield'].each do |y| %>
		        <td><%= y['value'].round %></td>
		      <% end %>
		    </tr>
		    <% end %>
			</tbody>
		</table>
  </div>
</div>

</div>
<script src="./js/libs/jquery-2.1.0.min.js"></script>
<script src="./js/libs/bootstrap.min.js"></script>

</body>
</html>
